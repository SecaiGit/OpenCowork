# Session Context

## 近期工作

### 待完成

- [ ] 聊天记录前端应该永远不会变化；压缩后的 context 应该保留在后台，并提供界面查看压缩后的数据。由于前端记录不再变化，也应该修复现在不断滚动到顶部再重新滚动的 bug，并优化性能，只加载可见部分的文档。
- [ ] 新增远程 App，用于手机开发。

### 已完成

- [x] Plan Mode 应该可以修改 `docs/` 下的文档和 `plans/` 下的文档，而不只是 `.plan/` 下的文档。

## 记录规范

- 本文件记录近期工作、待办事项和会话交接信息。
- 新增事项优先放入“待完成”；完成后移动到对应的完成记录中。
